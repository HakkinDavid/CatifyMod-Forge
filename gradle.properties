# Done to increase the memory available to gradle.
org.gradle.jvmargs=-Xmx1G
org.gradle.parallel=true

# ForgeGradle 7: merge client/common source sets (when present)
net.minecraftforge.gradle.merge-source-sets=true

# Forge / Minecraft Properties
minecraft_version=26.1
forge_version=62.0.1
mappings_channel=official
mappings_version=26.1

# Mod Properties
mod_version=1.0.2
maven_group=org.daylight
archives_base_name=catify

# Dependencies (optional)
# If we add extra compile deps later, list them here.